struct ACauto {
    const static int N = 2e5 + 5;
    int tr[26][N], fail[N], ctn = 0, cnt[N], endat[N];
    inline void add(const string &s) {
        }
    void blt(const vector<sring> &ar) {
    }
};